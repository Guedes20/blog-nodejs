<div class="jumbotron">
    <h1 class="display-4">Bem vindo ao Tecnoblog!!!</h1>
    <p class="lead">Neste blog iremos falar sobre tecnologias, serviços, projetos...</p>
    <hr class="my-4">
    <p>Ele usa classes utilitárias para tipografia e espaçamento de conteúdo, dentro do maior container.</p>
    <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
</div>

</hr>

<h2> Postagens recentes: </h2>

{{#each postagens}}
<div class="card mt-4">
  <div class="card-body">
  <h3>{{titulo}}</h3>
    <p>{{descricao}}</p>   
   <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button> </a>
  
   <hr>
   <small>Categoria :{{categoria.nome}} </small></br> 
   <small>Data de publicação :{{date}} </small>
   
  </div>
</div>
{{else}}
  <h4>Nenhuma postagem registrada</h4>
{{/each}} 
